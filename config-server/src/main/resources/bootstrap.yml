server:
  port: 8888

spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/microservice #classpath:/microservice load olacak..

  profiles:
    active: native